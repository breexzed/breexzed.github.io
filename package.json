{
  "name": "lens-map",
  "version": "1.0.0",
  "description": "Living topological lens — f(x) = y",
  "main": "index.html",
  "scripts": {
    "build": "npm run build:content && vite build",
    "build:content": "npm run build:topology && npm run copy:assets && npm run build:search && npm run build:ai",
    "build:topology": "node scripts/build-topology.js",
    "build:search": "node scripts/build-search.js",
    "build:ai": "node scripts/build-ai-index.js",
    "copy:assets": "node scripts/copy-assets.js",
    "dev": "npm run build:content && vite",
    "preview": "vite preview",
    "smoke:runtime": "node scripts/smoke-runtime.js",
    "smoke": "npm run build && npm run smoke:runtime",
    "deploy": "npm run build && git add data/topology.json public/data/topology.json public/data/search-index.json public/ai public/llms.txt && git commit -m 'Update topology content' && git push"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/furylight61-ops/lens-map.git"
  },
  "keywords": [
    "topology",
    "lens",
    "knowledge-graph",
    "markdown",
    "static-site"
  ],
  "author": "owlcyon",
  "license": "MIT",
  "dependencies": {
    "@vercel/speed-insights": "^2.0.0",
    "flexsearch": "^0.7.43",
    "glob": "^10.3.0",
    "gray-matter": "^4.0.3",
    "marked": "^11.0.0",
    "vite": "^5.4.21"
  },
  "devDependencies": {
    "@rollup/rollup-linux-x64-gnu": "^4.63.1",
    "@types/node": "^20.11.0",
    "http-server": "^14.1.1",
    "typescript": "^5.3.3"
  }
}
